#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='promptbot_overlapper',
      version='1.0',
      packages=find_packages(),
      scripts=['promptbot_overlapper.py']
    )
